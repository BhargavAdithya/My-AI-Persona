{
  "build": {
    "buildCommand": "pip install -r requirements.txt && python -m rag.ingest"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}